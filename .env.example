# ============================================================================
# OpenClaw Environment Configuration
# ============================================================================
# Copy this file to .env and uncomment the variables you need
# WARNING: DO NOT commit .env files - they contain secrets!
# ============================================================================
#
# This file documents all available environment variables for OpenClaw.
# Variables are organized by category for easy reference.
#
# For detailed information about environment variable precedence and usage,
# see: docs/environment.md
#
# ============================================================================

# ============================================================================
# DOCKER COMPOSE & CONTAINER CONFIGURATION
# ============================================================================

# ----------------------------------------------------------------------------
# DEVELOPMENT ENVIRONMENT
# ----------------------------------------------------------------------------
# Use these settings for local development and testing
# Runs on non-standard ports to avoid conflicts with production
# ----------------------------------------------------------------------------

# Docker image to use for gateway and CLI containers
# Default: ghcr.io/nikolasp98/openclaw:main
# OPENCLAW_IMAGE=ghcr.io/nikolasp98/openclaw:dev

# Container names for DEV environment
# Default: none (Docker generates names)
# OPENCLAW_GATEWAY_CONTAINER_NAME=openclaw_DEV_gw
# OPENCLAW_CLI_CONTAINER_NAME=openclaw_DEV_cli

# Environment identifier (used for logging and display)
# Default: none
# OPENCLAW_ENV=DEV

# Configuration and workspace directories
# Default: ~/.openclaw
# OPENCLAW_CONFIG_DIR=/home/nikolas/.openclaw-dev
# OPENCLAW_WORKSPACE_DIR=/home/nikolas/.openclaw-dev/workspace

# Gateway and bridge ports
# Default: 18789 (gateway), 18790 (bridge)
# OPENCLAW_GATEWAY_PORT=18788
# OPENCLAW_BRIDGE_PORT=18791

# Gateway bind address (loopback, lan, public)
# Default: loopback
# OPENCLAW_GATEWAY_BIND=lan

# Gateway authentication token (required for gateway access)
# Generate a secure random token for this value
# Default: none
# OPENCLAW_GATEWAY_TOKEN=<generate-random-dev-token>

# ----------------------------------------------------------------------------
# PRODUCTION ENVIRONMENT
# ----------------------------------------------------------------------------
# Use these settings for production deployments
# Uses standard ports and production image tag
# ----------------------------------------------------------------------------

# OPENCLAW_IMAGE=ghcr.io/nikolasp98/openclaw:prd
# OPENCLAW_GATEWAY_CONTAINER_NAME=openclaw_PRD_gw
# OPENCLAW_CLI_CONTAINER_NAME=openclaw_PRD_cli
# OPENCLAW_ENV=PRD
# OPENCLAW_CONFIG_DIR=/home/nikolas/.openclaw-prd
# OPENCLAW_WORKSPACE_DIR=/home/nikolas/.openclaw-prd/workspace
# OPENCLAW_GATEWAY_PORT=18789
# OPENCLAW_BRIDGE_PORT=18790
# OPENCLAW_GATEWAY_BIND=lan
# OPENCLAW_GATEWAY_TOKEN=<generate-random-prd-token>

# ----------------------------------------------------------------------------
# MULTI-TENANT PRODUCTION
# ----------------------------------------------------------------------------
# For multi-tenant deployments, each tenant gets isolated directories and containers.
# Naming convention: openclaw-prd-{tenant} for deployment, .openclaw-prd-{tenant} for config.
#
# Example for tenant "faces":
# OPENCLAW_IMAGE=ghcr.io/nikolasp98/openclaw:prd
# OPENCLAW_GATEWAY_CONTAINER_NAME=faces_openclaw_gw
# OPENCLAW_CLI_CONTAINER_NAME=faces_openclaw_cli
# OPENCLAW_ENV=PRD
# OPENCLAW_CONFIG_DIR=/home/deploy/.openclaw-prd-faces
# OPENCLAW_WORKSPACE_DIR=/home/deploy/.openclaw-prd-faces/workspace
# OPENCLAW_GOG_CONFIG_DIR=/home/deploy/.config/gogcli
# OPENCLAW_DOCKER_PLATFORM=linux/amd64
# OPENCLAW_GATEWAY_PORT=18789
# OPENCLAW_BRIDGE_PORT=18790
# OPENCLAW_GATEWAY_BIND=lan
# OPENCLAW_GATEWAY_TOKEN=<openssl rand -hex 32>

# ----------------------------------------------------------------------------
# Docker Advanced Options
# ----------------------------------------------------------------------------

# Docker platform constraint (prevents wrong architecture pulls)
# Set explicitly to match your server's CPU architecture
# Default: linux/amd64
# OPENCLAW_DOCKER_PLATFORM=linux/amd64
# OPENCLAW_DOCKER_PLATFORM=linux/arm64   # For ARM servers (Oracle Cloud, Raspberry Pi, Synology)

# Additional APT packages to install in Docker image at build time
# Format: space-separated package names
# Default: chromium fonts-liberation fonts-noto-color-emoji
# Set to empty string to disable browser support entirely
# Standard (default): chromium fonts-liberation fonts-noto-color-emoji
# Full (with VNC):    chromium fonts-liberation fonts-noto-color-emoji xvfb x11vnc novnc websockify socat
# OPENCLAW_DOCKER_APT_PACKAGES=chromium fonts-liberation fonts-noto-color-emoji

# Extra volume mounts for Docker containers
# Format: Docker mount specification
# Default: none
# OPENCLAW_EXTRA_MOUNTS=type=bind,source=/host/path,target=/container/path

# Custom home directory volume for persistence
# Default: Docker manages volumes
# OPENCLAW_HOME_VOLUME=openclaw-home

# ============================================================================
# AUTHENTICATION & GATEWAY
# ============================================================================

# Gateway authentication password (alternative to token)
# Default: none
# OPENCLAW_GATEWAY_PASSWORD=<secure-password>

# ============================================================================
# CLAUDE AI PROVIDER
# ============================================================================

# Claude.ai session credentials for web provider
# Obtain from claude.ai cookies after logging in
# Default: none (required for Claude web provider)
# CLAUDE_AI_SESSION_KEY=<your-session-key>
# CLAUDE_WEB_SESSION_KEY=<your-web-session-key>
# CLAUDE_WEB_COOKIE=<your-cookie>

# ============================================================================
# MESSAGING CHANNELS
# ============================================================================

# ----------------------------------------------------------------------------
# Telegram
# ----------------------------------------------------------------------------

# Telegram bot token from @BotFather
# Format: 1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
# Default: none
# TELEGRAM_BOT_TOKEN=<your-telegram-bot-token>

# Comma-separated list of Telegram user IDs for debug logging
# Used to enable debug mode for specific accounts during testing
# Default: none
# OPENCLAW_DEBUG_TELEGRAM_ACCOUNTS=123456789,987654321

# ----------------------------------------------------------------------------
# Discord
# ----------------------------------------------------------------------------

# Discord bot token from Discord Developer Portal
# Format: long alphanumeric string (store raw token, no prefix)
# Default: none
# DISCORD_BOT_TOKEN=<your-discord-bot-token>

# ----------------------------------------------------------------------------
# Feishu (Lark)
# ----------------------------------------------------------------------------

# Feishu/Lark application credentials
# Obtain from Feishu Open Platform
# Default: none
# FEISHU_APP_ID=<your-feishu-app-id>
# FEISHU_APP_SECRET=<your-feishu-app-secret>

# ============================================================================
# AI MODEL PROVIDERS
# ============================================================================

# ----------------------------------------------------------------------------
# OpenRouter
# ----------------------------------------------------------------------------

# OpenRouter API key for multi-model access
# Get from: https://openrouter.ai/keys
# Default: none
# OPENROUTER_API_KEY=<your-openrouter-api-key>

# ----------------------------------------------------------------------------
# Groq
# ----------------------------------------------------------------------------

# Groq API key for fast LLM inference
# Get from: https://console.groq.com/keys
# Default: none
# GROQ_API_KEY=<your-groq-api-key>

# ----------------------------------------------------------------------------
# Brave Search
# ----------------------------------------------------------------------------

# Brave Search API key for web search integration
# Get from: https://brave.com/search/api/
# Default: none
# BRAVE_API_KEY=<your-brave-api-key>

# ----------------------------------------------------------------------------
# ZAI (Z AI)
# ----------------------------------------------------------------------------

# ZAI API key (both formats supported)
# Default: none
# ZAI_API_KEY=<your-zai-api-key>
# Z_AI_API_KEY=<your-zai-api-key>

# ----------------------------------------------------------------------------
# Vercel Gateway
# ----------------------------------------------------------------------------

# Vercel Gateway API key for AI model routing
# Default: none
# VERCEL_GATEWAY_API_KEY=<your-vercel-gateway-api-key>

# ============================================================================
# TAILSCALE INTEGRATION
# ============================================================================

# Path to Tailscale socket for daemon communication
# Default: /var/run/tailscale/tailscaled.sock (Linux)
#          /var/run/tailscaled.socket (macOS)
# TAILSCALE_SOCKET=/var/run/tailscale/tailscaled.sock

# Path to Tailscale binary
# Default: /usr/bin/tailscale (Linux)
#          /Applications/Tailscale.app/Contents/MacOS/Tailscale (macOS)
# TAILSCALE_BINARY=/usr/bin/tailscale

# ============================================================================
# MEDIA UNDERSTANDING
# ============================================================================

# ----------------------------------------------------------------------------
# Whisper (Speech Recognition)
# ----------------------------------------------------------------------------

# Path to Whisper C++ model file for audio transcription
# Models: tiny, base, small, medium, large
# Default: looks in common locations
# WHISPER_CPP_MODEL=/path/to/ggml-model.bin

# ----------------------------------------------------------------------------
# Sherpa ONNX (Voice Activity Detection)
# ----------------------------------------------------------------------------

# Directory containing Sherpa ONNX model files
# Used for voice activity detection and audio processing
# Default: looks in common locations
# SHERPA_ONNX_MODEL_DIR=/path/to/sherpa-onnx-models

# ============================================================================
# RUNTIME & DEVELOPMENT
# ============================================================================

# ----------------------------------------------------------------------------
# Shell Environment
# ----------------------------------------------------------------------------

# Load shell environment variables from user profile
# Set to 'true' to enable, 'false' to disable
# Default: true (loads from ~/.profile, ~/.bashrc, ~/.zshrc)
# OPENCLAW_LOAD_SHELL_ENV=true

# Timeout in milliseconds for loading shell environment
# Default: 5000 (5 seconds)
# OPENCLAW_SHELL_ENV_TIMEOUT_MS=5000

# ----------------------------------------------------------------------------
# Node.js Execution
# ----------------------------------------------------------------------------

# Node.js execution mode (host or container)
# Default: auto-detect
# OPENCLAW_NODE_EXEC_HOST=true

# Fallback to host execution if container fails
# Default: true
# OPENCLAW_NODE_EXEC_FALLBACK=true

# ----------------------------------------------------------------------------
# Bundled Resources
# ----------------------------------------------------------------------------

# Override bundled OpenClaw version check
# Used for development builds
# Default: none
# OPENCLAW_BUNDLED_VERSION=dev

# Override bundled plugins directory
# Default: uses built-in bundled plugins
# OPENCLAW_BUNDLED_PLUGINS_DIR=/custom/plugins/dir

# Override bundled hooks directory
# Default: uses built-in bundled hooks
# OPENCLAW_BUNDLED_HOOKS_DIR=/custom/hooks/dir

# ----------------------------------------------------------------------------
# Directory Paths
# ----------------------------------------------------------------------------

# OAuth credentials directory
# Default: ~/.openclaw/oauth
# OPENCLAW_OAUTH_DIR=/custom/oauth/dir

# State directory for gateway and runtime data
# Default: ~/.openclaw
# OPENCLAW_STATE_DIR=/custom/state/dir

# Configuration profile name
# Used to manage multiple configurations
# Default: none (uses default profile)
# OPENCLAW_PROFILE=production

# ----------------------------------------------------------------------------
# Browser Integration
# ----------------------------------------------------------------------------

# Browser executable path for CLI commands that open web pages
# Used in Docker CLI container
# Default: system default browser
# BROWSER=/usr/bin/firefox

# ----------------------------------------------------------------------------
# Testing & Debug
# ----------------------------------------------------------------------------

# Enable live testing mode (requires API keys)
# Used by test suite for integration tests
# Default: false
# CLAWDBOT_LIVE_TEST=1
# LIVE=1

# ============================================================================
# MACOS SPECIFIC
# ============================================================================

# ----------------------------------------------------------------------------
# Legacy Variables (macOS)
# ----------------------------------------------------------------------------
# These variables are deprecated but still supported for backward compatibility
# Use the OPENCLAW_ prefixed versions instead

# Legacy gateway port (use OPENCLAW_GATEWAY_PORT instead)
# Default: 18789
# CLAWDBOT_GATEWAY_PORT=18789

# Legacy gateway bind (use OPENCLAW_GATEWAY_BIND instead)
# Default: loopback
# CLAWDBOT_GATEWAY_BIND=loopback

# ============================================================================
# OPTIONAL: HOST-SPECIFIC PATHS
# ============================================================================

# GOG CLI configuration directory (for Gmail automation via gogcli)
# Default: ~/.config/gogcli
# OPENCLAW_GOG_CONFIG_DIR=~/.config/gogcli
#
# Note: The container automatically fixes ownership of this directory on startup.
# No manual chown commands needed.

# ============================================================================
# NOTES
# ============================================================================
#
# Environment Variable Precedence (highest to lowest):
# 1. Environment variables set in shell/container
# 2. Variables from .env file (if using docker-compose)
# 3. Default values in code
#
# Security Notes:
# - Never commit .env files to version control
# - Use strong, randomly generated tokens for authentication
# - Keep API keys and credentials secure
# - Rotate credentials regularly
#
# Docker Compose Usage:
# - This file is automatically loaded by docker-compose
# - Uncomment the environment section you need (DEV or PRD)
# - Generate unique tokens for each environment
#
# For more information:
# - Environment variables: docs/environment.md
# - Docker setup: docker-setup.sh
# - Configuration: docs/configuration.md
#
# ============================================================================
