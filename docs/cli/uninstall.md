---
summary: "CLI reference for `minion uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `minion uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
minion uninstall
minion uninstall --all --yes
minion uninstall --dry-run
```
